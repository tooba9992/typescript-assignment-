"use strict";
/*greetings start with the same array of name above but instead of just
printing each person name printa message to them the text of each message
should be the same but each message should be personalized with the person name*/
let named = ["moona", "mehroz", "numra", "mujtaba", "hasnain"];
for (let name of named) {
    console.log(`Hello, ${name}! How are you doing today?`);
}
